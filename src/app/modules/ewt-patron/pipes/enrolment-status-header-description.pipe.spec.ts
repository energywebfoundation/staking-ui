import { EnrolmentStatusHeaderDescriptionPipe } from './enrolment-status-header-description.pipe';

describe('EnrolmentStatusHeaderDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new EnrolmentStatusHeaderDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
