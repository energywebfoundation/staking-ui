import { EnrolmentStatusLinkPipe } from './enrolment-status-link.pipe';

describe('EnrolmentStatusLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new EnrolmentStatusLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
